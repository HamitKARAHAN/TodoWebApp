@model WorkUpdateDto

<section class="get-in-touch">
    <h5 class="title">Update Todo</h5>
    @using (Html.BeginForm("Update", "Todo", FormMethod.Post, new { @class = "contact-form row" }))
    {
        @Html.HiddenFor(x => x.Id)
        <div class="form-field col-lg-12">
            @Html.TextBoxFor(x => x.Title, new { @class = "input-text js-input", required = "required" })
            @Html.LabelFor(x => x.Title, "Title", new { @class = "label" })
        </div>
        <div class="form-field col-lg-12">
            @Html.TextBoxFor(x => x.Description, new { @class = "input-text js-input", required = "required" })
            @Html.LabelFor(x => x.Description, "Description", new { @class = "label" })
        </div>
        <div class="form-field form check">
            @Html.CheckBoxFor(x => x.IsCompleted, new { @class = "input-text js-input"})
            @Html.LabelFor(x => x.IsCompleted, "Completed", new { @class = "label" })
        </div>
        <div class="form-field col-lg-12">
            <button class="submit-btn" type="submit">Save</button>
        </div>
    }
</section>
